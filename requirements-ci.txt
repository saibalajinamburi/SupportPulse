# CI-only requirements — lightweight packages for unit tests only.
# Does NOT include GPU packages (torch, chromadb, ollama) — those
# require GPU hardware and are not testable on CI runners.
# Full requirements are in requirements.txt for local development.

# Core ML & data
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.4.0
lightgbm>=4.0.0
scipy>=1.12.0

# API framework
fastapi>=0.111.0
pydantic>=2.7.0
httpx>=0.27.0

# Test framework
pytest>=8.0.0
pytest-anyio>=0.0.0

# Linting (installed separately in CI but listed for completeness)
ruff>=0.4.0
